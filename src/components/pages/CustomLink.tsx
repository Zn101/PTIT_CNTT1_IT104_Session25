import { NavLink } from "react-router-dom";

export default function CustomLink() {
  return (
    <div>
        <NavLink to="/login">
            Login
        </NavLink>
    </div>
  )
}
